from setuptools import setup, find_packages

setup(
    name='game',
    version="0.0.1",
    description="game: Game implementation for 0x2048",
    include_package_data=True,
    packages=find_packages(),
)
